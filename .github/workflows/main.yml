name: Build & Deploy Mint Turbo Monorepo

on:
  push:
    branches:
      - main

jobs:
  detect-changes:
    name: Detect changed turborepo packages
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      admin: ${{ steps.filter.outputs.admin }}
      ws: ${{ steps.filter.outputs.ws }}
    steps:
      - uses: actions/checkout@v4

      - name: Detect changes using paths-filter
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            api:
              - 'apps/mint-server/**'
            admin:
              - 'apps/mint-administration/**'
            ws:
              - 'apps/mint-ws-runners/**'

  # =========================================================
  # API BUILD
  # =========================================================
  build-api:
    name: Build & Push mint-api
    needs: detect-changes
    if: needs.detect-changes.outputs.api == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies (workspace)
        run: pnpm install --no-frozen-lockfile

      - name: Build API via Turborepo
        run: pnpm turbo run build --filter=mint-server...

      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker buildx push API
        run: |
          docker buildx build \
          --platform linux/amd64,linux/arm64 \
          --cache-from=type=registry,ref=charleschrismann/mint-api:buildcache \
          --cache-to=type=registry,ref=charleschrismann/mint-api:buildcache,mode=max \
          -f apps/mint-server/Dockerfile \
          -t charleschrismann/mint-api:latest \
          --push .


  # =========================================================
  # ADMIN FRONT BUILD
  # =========================================================
  build-admin:
    name: Build & Push mint-administration
    needs: detect-changes
    if: needs.detect-changes.outputs.admin == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies (workspace)
        run: pnpm install --no-frozen-lockfile

      - name: Build Administration via Turborepo
        run: pnpm turbo run build --filter=mint-administration...

      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker buildx push Administration
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --cache-from=type=registry,ref=charleschrismann/mint-administration:buildcache \
            --cache-to=type=registry,ref=charleschrismann/mint-administration:buildcache,mode=max \
            -t charleschrismann/mint-administration:latest \
            --push ./apps/mint-administration

  # =========================================================
  # WS RUNNERS BUILD
  # =========================================================
  build-ws:
    name: Build & Push mint-ws-runners
    needs: detect-changes
    if: needs.detect-changes.outputs.ws == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies (workspace)
        run: pnpm install --no-frozen-lockfile

      - name: Build WS-Runners via Turborepo
        run: pnpm turbo run build --filter=mint-ws-runners...

      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker buildx push WS runners
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --cache-from=type=registry,ref=charleschrismann/mint-ws-runners:buildcache \
            --cache-to=type=registry,ref=charleschrismann/mint-ws-runners:buildcache,mode=max \
            -t charleschrismann/mint-ws-runners:latest \
            --push ./apps/mint-ws-runners

  # =========================================================
  # DEPLOY
  # =========================================================
  deploy:
    name: Deploy to VPS
    needs: [build-api, build-admin, build-ws]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Deploy if something was built
        if: |
          needs.build-api.result == 'success' ||
          needs.build-admin.result == 'success' ||
          needs.build-ws.result == 'success'
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd mint/pie-mint/

            git checkout main
            git pull

            echo "Pulling latest images..."
            docker compose -f docker-compose.vps.yml pull

            docker exec mint-vps-api ./scripts/i.sh

            echo "Restarting services..."
            docker compose -f docker-compose.vps.yml up -d

      - name: Nothing to deploy
        if: |
          needs.build-api.result != 'success' &&
          needs.build-admin.result != 'success' &&
          needs.build-ws.result != 'success'
        run: echo "No build succeeded, skipping deployment."
